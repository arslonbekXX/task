# This project currently needs no environment variables.
# Map tiles come from CartoDB (OpenStreetMap data) and require no API key.
# Add VITE_-prefixed variables here when needed — Vite only exposes VITE_* to the browser.
